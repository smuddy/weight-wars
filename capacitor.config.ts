import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.fatfighters.app',
  appName: 'fat-fighters',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
